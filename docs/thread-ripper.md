# 线程撕裂者下载层

参考 Bilibili-thread-ripper **0.9.4.2** 正式版（上游提交
`847848a412ae8c648a95367972bb338d8196efcb`）移植共享下载核心：
https://github.com/MrTangLuyao/Bilibili-thread-ripper

参考 `src/range-core.js`、`src/cdn-resolver.js`、`src/idm-downloader.js`。
MIT 版权声明见 [许可证](licenses/bilibili-thread-ripper.txt)。

保留 PiliPlus 的 media_kit/mpv 播放器、解码、弹幕、字幕、画质、音轨、倍速、
后台播放与进度控制。浏览器 userscript 的页面注入和 MSE 接管不适用于原生应用；
这里通过只监听 127.0.0.1、带随机会话路径的 HTTP Range 代理接入同样的下载方式。
不运行旧版本替换播放器的代码。

- 默认开启，默认海外节点、8 个并发连接。在「设置 → 音视频设置」的 CDN 设置下方可关闭、改地区或改并发数；下次加载生效，保留既有配置。
- 新增可选自动并发数，按上游源码默认关闭。开启后在 8/12/16/24/32 间调整：卡顿或低缓冲压力触发试升，10 秒无收益回退并休息 90 秒，412/429 回退并冷却 180 秒。按完整分块计有效吞吐，副本重复字节不计入；同一播放器切视频保留学习结果。
- 新增自定义节点，非空时只使用所选服务器（包括元数据探测）；清空恢复大陆/海外模式。最多 32 个，按上游媒体域名白名单校验，签名地址不会发送给任意网站。
- DASH 音视频共享连接预算。8 并发时普通请求最多占 7 个连接，保留 1 个救援连接；
  初始视频分块预算 6、音频预算 1。队列按原版优先级排序，同优先级按进入顺序调度。
- 元数据按 0/120/300 ms 启动最多三个副本；首个 64 KiB 媒体块立即抢跑最多八个候选。
  启动后续块按已证实节点的速度分配，250 ms 后启动备用副本；常规块的延迟根据实测块耗时调整为 250–900 ms。
  成功后取消其余副本。首响应超时 5.5 秒、流停滞 4 秒、单次尝试 15 秒，
  最多三轮重试，轮间等待 500/1000 ms。
- 节点选择对应原版 `startupCandidates`、`rangeCandidates`、`rescueCandidates`、
  `ordered` 和 `pieceCandidates`：包含海外四轮/大陆一轮预热、分块候选交错、
  健康度排序、轮换以及下载期间重新检查封禁。预热后最多选择六个节点，保留未测速节点的探索位置。
- 按上游平滑加权轮询分配子块，快节点多领任务；慢于最快节点十二分之一的节点暂不分配主请求。测速 90 秒后过期重试，换签名保留同节点/文件的速度历史，至少 48 KiB 的传输才更新测速。备份副本优先使用最快已知节点。
- 重试和备用副本保留已校验响应中连续收到的前缀，达到 32 KiB 时只请求缺失尾部。副本取得连接后才确定续传起点；跨节点仍校验总长度，超长/错误区间响应不进入可续传前缀。
- 严格校验 HTTP 206、Content-Range、分块实际长度和跨节点资源总长度；拒绝 200、
  错区间、截断、超长响应。错误不会作为媒体数据交给播放器。
- 使用上游的大陆/海外节点名单、签名路径保留、Akamai-only 地址合成、失败节点退避及每视频封禁（区分节点、地址与节点/地址组合）。
  元数据候选包括原始 API 地址；媒体候选按原版地区规则过滤。
- 原生适配边界：mpv 决定读取、拖动及缓冲，不能直接复用浏览器的 SIDX/MSE
  SourceBuffer 调度。代理将开放式 Range 拆成约 2 MiB 的视频窗口，按预算分块，
  最小块按连接测速自适应为 64 KiB–1 MiB，最终分块同时考虑窗口长度和连接预算；音频窗口为每个预算连接 64 KiB。
  每输出一块立即补充下一块，保留背压且不缓存整部视频。原版按 SIDX 段大小分块，
  原生代理则按字节窗口分块；两者并非逐请求相同。
- 额外的原生保护：普通连接上限在整个代理会话统一执行，避免多个开放式 Range
  同时下载时挤占救援连接。成功读完的 HTTP 请求不再调用 abort，允许连接复用。
- 拖动断开 HTTP 请求会取消该请求的下载；更换播放源或关闭播放器会取消整个代理会话。
- 本地文件、直播、非 DASH 视频继续走原生直连。启动代理失败时使用原始播放地址；
  下载耗尽重试时返回 HTTP 错误，由播放器现有错误/重试处理接管。
- 适配范围不是整份 userscript：网页悬浮按钮、网页设置入口、MSE 会话/缓冲修复、网页 HLS 直播模块和网页播放清单地址刷新不接入原生播放器。与上游兼容模式/桌面客户端一样，原生字节请求没有 SIDX 分段的播放截止时间，采用无 deadline 分支的调度，不虚构时间戳套用 0.9.4.1 的网页截止时间对冲策略。DASH 地址生命周期仍由 PiliPlus 管理。
- 原有卡顿定时器、画面冻结检测、自动修改 CDN 设置、重载和撤销提示均已删除。
  旧置顶、排序、自定义 CDN 弹窗及其配置读取代码也已删除，手动 CDN 界面恢复上游实现。
  线程撕裂者启用时独立选择 DASH 下载节点。旧版存储的置顶键不再被读取。
- 保留独立的 URL 正确性修复及其测试：音频 CDN 绕过、P2P 地址切换时使用 HTTPS
  并清除非标准端口、缺少主机参数时的安全回退。这些代码不包含卡顿检测或自动轮换。

回归测试：`flutter test test/thread_ripper_test.dart test/thread_ripper_auto_test.dart test/video_utils_test.dart`。
测试使用本地 HTTP 服务模拟正常、慢、拒绝、错误 Range、截断、资源大小变化和取消。

## 与原版的对照验证

`test/fixtures/ripper_resolver.json` 由原版 JavaScript 直接产生，覆盖两种地区的
候选生成、预热、成功/失败后的排序、采样过期和轮换；Dart 测试逐步核对这些结果。
`ripper_assignments.json` 对照原版任务分配，`ripper_auto.json` 对照自动并发试升/回退/限流/缓冲压力。
更新参考数据：`node tool/generate_ripper_resolver_fixture.cjs <上游源码目录>`。
另有八个并发读取同时遭遇媒体节点停滞的回归用例，验证救援仍能启动且本地活动请求不超过 8。

2026-09-24：上述三个测试文件共 31 项通过，修改涉及的 Dart 文件静态检查通过。
新增续传用例分别覆盖截断后重试、主请求仍传输时备用节点从已收到的位置接续，并逐字节核对完整结果。
这些是本地模拟 CDN 回归，不代表已经在用户平板上完成真实网络性能对比。

以下是 0.9.1.4 移植时期的历史测量，不代表 0.9.4.2 性能：
2026-09-20 在本机对 BV1WHe364EC2 的公开 DASH 流进行前 2 MiB 下载交叉测试，
海外、8 并发、同一签名地址及 HTTP Range，顺序为旧/新/新/旧：

| 实现 | 首包（ms） | 完成 2 MiB（ms） |
| --- | --- | --- |
| 旧实现，第 1 次 | 890 | 5167 |
| 本次实现，第 1 次 | 271 | 453 |
| 本次实现，第 2 次 | 309 | 483 |
| 旧实现，第 2 次 | 522 | 4823 |

另直接在 Node 中运行原版 `idm-downloader.js`，相同地址/区间/海外八线程、
启动模式、逐块回调：两次首包 140/39 ms，总耗时 601/260 ms。
这是下载层抽样对照，不是浏览器端到端播放测试；JS 与 Dart 的 HTTP 栈、连接复用
和缓存预热不同，原版核心测量也不包含原生代理的一次资源长度探测。
公开接口返回的轨道为 id=32、codec=12、176968 bit/s，未使用用户登录信息。
不能将这些结果当作平板上用户所选高画质、音视频同步或完整 284 秒播放的验证。
